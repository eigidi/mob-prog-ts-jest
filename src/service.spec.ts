import calc from './service';

describe('Calc', () => {
  
  test('should ...', () => {
    const input = 0;
    const expected = "";
    expect(calc.generateFizzBuzz(input)).toBe(expected);
  });


});
